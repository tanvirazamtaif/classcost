node_modules
dist
.env
*.local
.DS_Store

# Runtime-uploaded feed images (host storage)
uploads/

/src/generated/prisma

# AI tool bundles (for sharing code with ChatGPT/Claude/etc.) — never commit.
repomix-output.*
repopack-output.*
